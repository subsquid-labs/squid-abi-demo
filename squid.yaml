manifestVersion: subsquid.io/v0.1
name: colonial-pink
version: 1
description: ABI squid demo
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]
